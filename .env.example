# Optional: leave empty to use local Ollama
OLLAMA_API_KEY=

# Used by the Docker container to reach Ollama on Ubuntu
OLLAMA_HOST=http://host.docker.internal:11434

# Store ChromaDB indexes in the Docker volume
CHROMA_PERSISTENT=true
CHROMA_DIRECTORY=/app/chroma_db